<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Game Admin Login</title>

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;600&display=swap" rel="stylesheet">

  <style>
    body {
      min-height: 100vh;
      background: radial-gradient(circle at top, #1f0036, #000);
      display: flex;
      align-items: center;
      font-family: 'Orbitron', sans-serif;
      color: #fff;
    }

    .login-card {
      background: rgba(255, 255, 255, 0.08);
      backdrop-filter: blur(12px);
      border-radius: 18px;
      padding: 30px;
      box-shadow: 0 0 30px rgba(0, 255, 255, 0.25);
      border: 1px solid rgba(255, 255, 255, 0.15);
    }

    .login-title {
      text-align: center;
      font-size: 24px;
      margin-bottom: 10px;
      color: #00f7ff;
    }

    .login-sub {
      text-align: center;
      font-size: 13px;
      color: #ccc;
      margin-bottom: 25px;
    }

    .form-label {
      font-size: 13px;
      color: #ddd;
    }

    .form-control {
      background: rgba(0, 0, 0, 0.6);
      border: 1px solid #333;
      color: #fff;
      border-radius: 10px;
    }

    .form-control:focus {
      background: rgba(0, 0, 0, 0.7);
      color: #fff;
      border-color: #00f7ff;
      box-shadow: 0 0 8px rgba(0, 247, 255, 0.6);
    }

    .btn-login {
      background: linear-gradient(135deg, #00f7ff, #007bff);
      border: none;
      color: #000;
      font-weight: 600;
      border-radius: 25px;
      padding: 12px;
      transition: all 0.3s ease;
    }

    .btn-login:hover {
      transform: translateY(-2px);
      box-shadow: 0 0 15px #00f7ff;
    }

    .error-text {
      display: block;
      text-align: center;
      color: #ff4d4d;
      margin-bottom: 15px;
      font-size: 13px;
    }

    .admin-icon {
      width: 70px;
      height: 70px;
      background: linear-gradient(135deg, #00f7ff, #007bff);
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto 15px;
      font-size: 30px;
      color: #000;
    }
  </style>
</head>

<body>

<div class="container">
  <div class="row justify-content-center w-100">
    <div class="col-md-4 col-sm-10">

      <div class="login-card">

        <div class="admin-icon">🎮</div>

        <div class="login-title">Game Admin Login</div>
        <div class="login-sub">Secure access to game control panel</div>

        <% if (error) { %>
          <span class="error-text"><%= error %></span>
        <% } %>

        <form action="/login" method="POST">

          <div class="mb-3">
            <label class="form-label">Admin Email</label>
            <input type="email" name="email" class="form-control" placeholder="admin@game.com" required>
          </div>

          <div class="mb-4">
            <label class="form-label">Password</label>
            <input type="password" name="password" class="form-control" placeholder="••••••••" required>
          </div>

          <button type="submit" class="btn btn-login w-100">
            LOGIN
          </button>

        </form>

      </div>

    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
